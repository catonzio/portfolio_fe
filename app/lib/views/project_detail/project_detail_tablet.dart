
import 'package:flutter/material.dart';

class ProjectDetailTablet extends StatelessWidget {
  const ProjectDetailTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    	color: Colors.red,
    	child: const Column(
    	children: [
    		Text("ContactTablet", style: TextStyle(fontSize: 50)),
    	])
    );
  }
}
